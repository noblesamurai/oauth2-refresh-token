{
  "name": "oauth2-refresh-token",
  "version": "4.0.0",
  "description": "Keeps oauth2 auth token refreshed, handles refresh token management.",
  "main": "index.js",
  "engines": {
    "node": "> 8.x"
  },
  "scripts": {
    "pretest": "semistandard",
    "test": "mocha --recursive test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/noblesamurai/oauth2-refresh-token.git"
  },
  "keywords": [
    "token",
    "refresh"
  ],
  "author": "Tim Allen",
  "license": "BSD-3-Clause",
  "bugs": {
    "url": "https://github.com/noblesamurai/oauth2-refresh-token/issues"
  },
  "homepage": "https://github.com/noblesamurai/oauth2-refresh-token#readme",
  "dependencies": {
    "debug": "^2.2.0",
    "knex": "^0.12.1",
    "pg": "^6.1.0",
    "simple-oauth2": "^1.0.0"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^3.0.2",
    "nock": "^8.0.0",
    "semistandard": "^13.0.1"
  },
  "semistandard": {
    "env": [
      "mocha"
    ]
  }
}
